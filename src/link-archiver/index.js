import {
	pushToArchiveIs,
	pushToRobustLinks,
	pushToWayback,
} from "../link-archiver";
import { generateArchiveLink } from "./generateArchiveLink";

export default {
	generateArchiveLink,
	pushToArchiveIs,
	pushToRobustLinks,
	pushToWayback,
};
